server:
  port: 8081
logging:
  config: classpath:logback.xml

spring:
  application:
    name: simple_config_client
  jmx:
    default-domain: simple_config_client_default
  cloud:
    config:
      enabled: true
      uri: http://localhost:8080

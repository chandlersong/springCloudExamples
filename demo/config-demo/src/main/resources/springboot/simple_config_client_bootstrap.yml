server:
  port: 8081
logging:
  config: classpath:logback.xml

spring:
  application:
    name: foo
  jmx:
    default-domain: simple_config_client1
  profiles:
    active: development  # one way to define profile
  cloud:
    config:
      enabled: true
      uri: http://localhost:8080
      profile: development # one way to define profile
